# openspec/config.yaml — OllamaChat project configuration
# Auto-generated by sdd-init on 2026-06-22
schema: spec-driven

context: |
  Project: OllamaChat
  Tech stack: Python 3.12 (managed via UV), wxPython 4.2+, accessible-output2 0.17+, requests 2.31+
  Architecture: Desktop GUI app for Windows 11, targeting blind users via accessible-output2
  Testing: pytest (to be installed), strict TDD enabled
  Style: Standard Python with type hints
  Dev environment: WSL + UV (Python 3.12), GUI windows cannot run locally; smoke/test only

strict_tdd: true

testing:
  runner: pytest
  runner_command: uv run pytest
  layers:
    unit:
      available: false
      tool: pytest (to be installed via UV)
    integration:
      available: false
      tool: — (to be determined)
    e2e:
      available: false
      tool: — (GUI app — manual testing on Windows or smoke via pyautogui)
  coverage:
    available: false
    tool: pytest-cov (to be installed)
    command: uv run pytest --cov
  quality:
    linter:
      available: false
      tool: ruff (to be installed)
      command: uv run ruff check
    type_checker:
      available: false
      tool: mypy (to be installed)
      command: uv run mypy src/
    formatter:
      available: false
      tool: ruff (to be installed)
      command: uv run ruff format

delivery:
  strategy: single-pr-default
  review_budget_lines: 400
  chain_strategy: not_applicable

execution:
  mode: interactive
  artifact_store: openspec

rules:
  proposal:
    - Include rollback plan for risky changes
    - Keep scope minimal and focused on blind-user accessibility
  specs:
    - Use Given/When/Then for scenarios
    - Use RFC 2119 keywords (MUST, SHALL, SHOULD, MAY)
    - Accessibility requirements MUST be spec'ed from user stories
  design:
    - Include sequence diagrams for complex flows
    - Document architecture decisions with rationale
    - wxPython GUI considerations: no GUI in WSL; design for testability
  tasks:
    - Group by phase, use hierarchical numbering
    - Keep tasks completable in one session
  apply:
    tdd: true
    test_command: uv run pytest -xvs
    rules:
      - Follow existing code patterns (none yet — establish project conventions)
  verify:
    test_command: uv run pytest -xvs
    build_command: ""
    coverage_threshold: 0
  archive:
    - Warn before merging destructive deltas
